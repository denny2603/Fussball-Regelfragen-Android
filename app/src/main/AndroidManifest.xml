<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.simontenbeitel.regelfragen">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RegelfragenApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.activity.StartupActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.SingleQuestionActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"/>
        <activity
            android:name=".ui.activity.ExamActivity"
            android:label="@string/title_exam" />
        <activity
            android:name=".ui.activity.SettingsActivity"
            android:label="@string/title_settings" />
        <activity android:name=".ui.activity.AssessedExamActivity"
            android:label="@string/title_evaluated_exam"
            android:launchMode="singleTop"/>
        <activity
            android:name=".ui.activity.AssessedQuestionActivity"
            android:parentActivityName=".ui.activity.AssessedExamActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activity.AssessedExamActivity" />
        </activity>

        <service
            android:name=".network.service.RegelfragenService"
            android:exported="false" />
        <provider
            android:name=".database.RegelfragenContentProvider"
            android:authorities="de.simontenbeitel.regelfragen.provider"
            android:exported="false" />
    </application>

</manifest>
